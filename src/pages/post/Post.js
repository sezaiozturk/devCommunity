import {View, Text} from 'react-native';
import React from 'react';

const Post = () => {
  return (
    <View style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
      <Text style={{fontSize: 50}}>Post Page</Text>
    </View>
  );
};

export default Post;
