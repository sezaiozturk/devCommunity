import {View, Text, Button, Image} from 'react-native';
import React, {useState} from 'react';

const Gallery = () => {
  return (
    <View style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
      <Text style={{fontSize: 50}}>Gallery Page</Text>
    </View>
  );
};

export default Gallery;
